# Rust
/target/
**/target/
Cargo.lock

# LaTeX build artifacts
*.aux
*.fdb_latexmk
*.fls
*.synctex.gz

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local / env
.env
.env.local
*.local.json

# ARIS deployment (per-machine local config, not committed)
# Installed via `bash /path/to/ARIS/tools/install_aris.sh v2`.
# Re-run on each clone to get a working ARIS skill set.
.claude/
.aris/
CLAUDE.md
